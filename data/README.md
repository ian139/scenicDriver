# Data Layout

This is a clean skeleton data layout.

- `data/raw/images/`: raw satellite and terrain tiles
- `data/raw/labels.csv`: weak labels (heuristic)
- `data/raw/labels_human.csv`: manual scenic annotations
- `data/processed/`: generated features, reports, and run outputs

Keep large data out of git.
